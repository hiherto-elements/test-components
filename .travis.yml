sudo: true
dist: trusty
language: node_js
node_js: 8.9

cache:
  directories:
    - node_modules
    - bower_components

addons:
  firefox: latest
  chrome: stable

install:
  - npm install

before_script:
  - xvfb-run -s '-screen 0 1024x768x24' wct;

script:
  - npm test
